id: renda-fixa
name: Renda Fixa
description: CDBs, LCIs e LCAs
base_currency: BRL
status: active

rules:
  allowed_asset_types:
    - CDB
    - LCI
    - LCA

sources:
  - type: fixed_income_csv
    enabled: true

import:
  move_processed_files: true
  deduplicate_by:
    - institution
    - product_name
    - application_date
    - maturity_date
    - principal_applied_brl
