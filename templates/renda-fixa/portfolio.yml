id: renda-fixa
name: Renda Fixa
description: CDBs, LCIs, LCAs, Tesouro Direto e outros títulos de renda fixa
base_currency: BRL
status: active

rules:
  allowed_asset_types:
    - bond
    - treasury

sources:
  - type: broker_csv
    enabled: true

import:
  move_processed_files: true
  deduplicate_by:
    - source
    - external_id
    - operation_date
    - asset_code
    - operation_type
